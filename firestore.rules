rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access for store catalog, orders, settings, and categories
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
